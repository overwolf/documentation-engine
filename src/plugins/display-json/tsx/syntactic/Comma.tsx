import React from 'react';
import { classnames } from '../../theme';

// eslint-disable-next-line @docusaurus/no-untranslated-text
const Comma = () => <span className={classnames.comma}>,</span>;

export default Comma;
